Предлагаемая структура раздела
2.2. Архитектура библиотеки
2.2.1. Высокоуровневое описание архитектуры
    - для какого подхода создана (хореография), почему (обеспечивает легкое встраивание);
    - для каких систем она рассчитана: микросервисные системы с распределенными бизнес-процессами, событийным  взаимодействием и отдельными БД сервисов;
    - место библиотеки в системе: библиотека встраивается в сервисы, а не требует отдельной платформы;
    - связь архитектуры с нефункциональными требованиями: простота интеграции.
    - ограничения и компромиссы (библиотека все же подходит для систем которые уже работают с событийно-ориентированной архитектурой, наследуем все проблемы восстановления после сбоя при хореографии, которые были описаны в анализе предметной области)
    - UML-диаграмма классов и интерфейсов
Step, Message, Controller, Executor;
2.2.2. Архитектурный стиль и инверсия управления
    - инверсия управления как базовый принцип проектирования;
    - что именно контролирует библиотека, а что остается за пользователем;
связь с нефункциональными требованиями: расширяемость, независимость от стека, простота внедрения.
2.2.3. Публичный API и доменные абстракции
    - назначение основных сущностей: Step, Message, Controller, Executor;
    - зачем нужна каждая абстракция;
    - как эти сущности формируют основной интерфейс работы с библиотекой;
    - какие функциональные требования закрывает API;
    - какие нефункциональные требования поддерживаются через API:.
2.2.4. Инфраструктурные абстракции
    - интерфейсы над брокером сообщений (описание);
    - интерфейсы над БД (описание);
    - принципы проектирвания интерфейсов, почему именно такие
    - какую проблему решают - возможность подключить разные базы данных и брокеры для работы с разной архитектурой, убрать зависимость от конкретного стэка
    - ограничение текущего подхода: поддержка SQL-БД за счет опоры на database/sql.
2.2.4. Как обеспечивается транзакционность шага
    - описание интерфейса над транзакцией
    - применение паттернов transaction manager в go
    - описание интерфейсов
    - uml диаграмма интерфейсов
    - отдаем управление транзакцией на сторону библиотеки
    - какую проблему решаем - позволяем пользователю не думать об инфраструктурном слое и описывать только бизнес логику не прибегая к вызову транзакций, а только к вызову запросов, библиотека выполнит шаг внутри одной транзакции
2.2.5. Маршрутизация и схема движения сообщений
    - описание конфигурации роутинга;
    - как пользователь задает маршруты, топики и направления передачи сообщений;
    - использование saga_id
    - как библиотека берет на себя публикацию и прием сообщений;
    - какую проблему это решает: отделение бизнес-логики от транспортной логики;
    - связь с требованиями: простота интеграции, расширяемость, снижение связности
    - какую проблему решаем: абстрагируем пользователя от обратки входящих сообщений, даем пользователю место для декларативного описания пути сообщений
    
2.2.7. Обеспечение надежности и устойчивости
    - разделение инфраструктурных и бизнес-ошибок;
    - инфраструктурные retry и логические retry;
    - возможность пользовательской обработки бизнес-ошибок;
    - Inbox и Outbox как базовые механизмы надежности передачи сообщений;
    - дедупликация, повторная доставка, компенсационные сообщения;
    - связь с функциональными и нефункциональными требованиями: надежность, корректность, отказоустойчивость.
    - повторная доставка и идемпотентная обработка.

2.2.8. Наблюдаемость и средства контроля выполнения
 - логирование;
 - трассировка;
 - метрики Prometheus;
 - как наблюдаемость помогает анализировать сбои и восстанавливать консистентность;
 - какую проблему это решает: прозрачность состояния Saga и возможность ручного анализа.

2.2.9. Хранение состояния и структура таблиц
    - какие таблицы создаются библиотекой;
    - почему было принято решение делать библиотеку stateless
    - подход к созданию таблиц для разных SQL-БД;
    - структура таблиц outbox и inbox; ERD диаграмма
    - роль этих таблиц в надежной доставке и дедупликации;

2.2.6. Механизм выполнения шага Saga
    - описание входных точек
    - описание процесса выполнения шага
